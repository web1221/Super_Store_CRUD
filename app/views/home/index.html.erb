<h2 class='index-welcome'>Welcome to the Family!</h2>
<h4>Mario's Super Store is a one stop shop til you drop. You can find all your needs here... From Organic Candles to Jazz posters to the occasional Monster! Never shop anywhere else again!</h4>
<div class="row">
  <div class="col-sm-4 col-border">
    <h4 class="heading-border">Here is our most reviewed product:</h4>
    <% if @products.any? %>
    <ul>
      <% @products.most_reviewed.each do |p| %>
      <li><%= link_to p.name, product_path(p) %></li>
      <% end %>
    </ul>
    <% else %>
    <p>There are no products with the most reviews.</p>
    <% end %>
  </div>
  <div class="col-sm-4 col-border">
    <h4 class="heading-border">Here are products most recently added:</h4>
    <% if @products.any? %>
    <ul>
      <% @products.three_most_recent.each do |p| %>
      <li><%= link_to p.name, product_path(p) %></li>
      <% end %>
    </ul>
    <% else %>
    <p>There are no recent products.</p>
    <% end %>
  </div>
  <div class="col-sm-4 col-border">
    <h4 class="heading-border">Here are products made in the United States:</h4>
    <% if @products.any? %>
    <ul>
      <% @products.usa_product.each do |p| %>
      <li><%= link_to p.name, product_path(p) %></li>
      <% end %>
    </ul>
    <% else %>
    <p>There are no products made in the United States yet.</p>
    <% end %>
  </div>
</div>
